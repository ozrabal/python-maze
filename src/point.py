class Point:
    def __init__(self, x=0, y=0):
        self.x = x  # x-coordinate (horizontal) in pixels, x=0 is the left of the screen
        self.y = y  # y-coordinate (vertical) in pixels, y=0 is the top of the screen